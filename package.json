{
  "name": "logic-gate-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "test": "node --test",
    "lint": "node --check src/domain.js && node --check src/app.js && node --check test/domain.test.js && node --check scripts/build.mjs",
    "typecheck": "npm run lint",
    "build": "node scripts/build.mjs"
  }
}
